# Agents Service Dependencies - ONLY PydanticAI agents
# This container should be lightweight (~200MB)

# PydanticAI for agent framework
pydantic-ai>=0.0.13
pydantic>=2.0.0

# Web framework for agent server
fastapi>=0.104.0
uvicorn>=0.24.0

# HTTP client for MCP tool calls
httpx>=0.24.0

# Basic utilities
python-dotenv>=1.0.0

# Logging (lightweight)
structlog>=23.1.0

# Required for DeepConf confidence engine
numpy>=1.24.0

# NOTE: Limited ML libraries for DeepConf only
# - NO sentence-transformers
# - NO OpenAI SDK (agents use MCP tools)  
# - NO database clients (agents use MCP tools)
# - NO embeddings libraries
# All heavy ML and data operations happen in the Server service